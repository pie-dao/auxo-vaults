{
  "name": "mono-vaults",
  "version": "0.0.0",
  "devDependencies": {
    "prettier": "^2.5.1",
    "prettier-plugin-solidity": "^1.0.0-beta.19",
    "pretty-quick": "^3.1.3"
  },
  "scripts": {
    "lint": "solhint \"contracts/**/*.sol\"",
    "format": "prettier --write contracts/**/*.sol interfaces/**/*.sol",
    "format:check": "prettier --check contracts/**/*.sol interfaces/**/*.sol"
  }
}
